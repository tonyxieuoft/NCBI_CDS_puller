Pulls coding sequences from transcripts housed in NCBI's nucleotide database.

As input, takes a directory of .txt files, each containing NCBI accessions (one per line) for the transcripts from which coding sequences will be extracted.

Outputs a directory containing fasta files with the coding sequences. 
